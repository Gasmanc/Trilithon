# .

_(one-line description)_

## Stack

- **rust** (workspace-three-layer) at `core/`
- **typescript** (react-frontend) at `web/`
- **swift** (xcode-app) at `app/`


## Getting started

```bash
just check       # run all linters + tests
just test        # run tests only
just fmt         # format all code
just fix         # auto-fix linter findings where possible
```

## Layout

Multi-component layout:

- **rust** (workspace-three-layer) at `core/`
- **typescript** (react-frontend) at `web/`
- **swift** (xcode-app) at `app/`


See `CLAUDE.md` for conventions and `docs/` for architecture and planning.
