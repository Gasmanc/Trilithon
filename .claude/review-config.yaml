# Review workflow configuration — read by review-aggregate, review-remediate, review-triage
# Paths are relative to the project root.
version: 1
in_flight_dir: docs/In_Flight_Reviews
findings_dir: docs/End_of_Phase_Reviews/Findings
fixed_dir: docs/End_of_Phase_Reviews/Fixed
unfixed_dir: docs/End_of_Phase_Reviews/Unfixed
aggregate_dir: docs/Codebase_MultiReview_Aggregate

# ─── Cross-phase coherence (added by cross-phase-scaffold) ───
cross_phase:
  # Set by `cargo xtask set-merge-review-baseline` — adoption SHA.
  # /where rule 12 only fires for commits AFTER this SHA.
  merge_review_baseline_sha: 0a795583ea9c4266e7d9b0ae0f56fd47d2ecf574
  finding_schema_required: true
  contract_registry_required: true
  seam_registry_required: true
